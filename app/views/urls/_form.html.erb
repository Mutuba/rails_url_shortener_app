<%= form_with(model: url) do |form| %>
  <% if url.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(url.errors.count, "error") %> prohibited this url from being saved:</h2>

      <ul>
        <% url.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :long_url, style: "display: block" %>
    <%= form.text_field :long_url %>
  </div>

  <div>
    <%= form.label :short_url, style: "display: block" %>
    <%= form.text_field :short_url %>
  </div>

  <div>
    <%= form.label :click, style: "display: block" %>
    <%= form.number_field :click %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
